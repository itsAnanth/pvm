# PyPath

A simple utility script meant to make python version management easier.

bootstraps latest python builds to generate pip.exe and adds python.exe and scripts/pip.exe to path setting it as default